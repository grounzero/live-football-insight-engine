{
  "name": "football-insights-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:ssr": "vite build --ssr test/layout.test.jsx --outDir .ssr-test --emptyOutDir && vite build --ssr test/playout.test.jsx --outDir .ssr-test/playout --emptyOutDir && node --test .ssr-test/layout.test.js .ssr-test/playout/playout.test.js"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.5",
    "vite": "^8.2.0"
  }
}
